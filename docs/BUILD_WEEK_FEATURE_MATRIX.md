# Feature and quirk matrix

This matrix is maintained as a current subsystem inventory. Historical counts
remain in [the Build Week record](BUILD_WEEK.md); future plans do not count as
implemented rows here.

`K`, `M`, and `C` mean computer keyboard, mouse, and configured controller.
The controller action source of truth is `src/navigation.rs`; detailed bindings
are in `CONTROLLER_INTERFACE.md`.

| Subsystem | Purpose and workflow | Access | Configuration | Persistent data | Offline/failure behavior | Safety and important limitations | Architecture / tests | Best shot |
|---|---|---|---|---|---|---|---|---|
| Preset browser and three engine catalogs | Browse synthv1 files, Yoshimi banks, or FluidSynth SoundFont programs; load one sound | K/M/C | Engine commands, clients, output matches, roots, categories, SoundFonts | Public synthv1 presets; external banks stay in place | Unavailable engines remain visible with a reason; browser opens without JACK | Only one managed engine; engine-specific preset/control rules never mix | `preset.rs`, `engine.rs`; schema, discovery, SoundFont, preflight tests | `shr-daw-presets.png` |
| Managed engine lifecycle | Preflight, start/reuse, connect MIDI/JACK, stop or replace the owned synth | K/C load, global panic; CLI | `synth.*`, `synthv1.*`, `yoshimi.*`, `fluidsynth.*`, `audio.*` | PID/start-time/executable ownership marker and log in state dir | Start errors stay visible and invite retry; crashed engine clears the loaded state | Sends All Notes Off; verifies PID, start time, and executable; never kills an unowned synth; no engine layering | `engine.rs`, `main.rs`; ownership, routing, cleanup tests | Playback plus physical setup |
| Live MIDI, controls, chord/keyboard display, and pickup | Consume controls/commands, pass musical MIDI, show held notes/chords and continuous keyboard state, move 12 synthv1 values | Musical input; K/M/C navigation; mapped CCs | MIDI input match, controller mappings, synth MIDI output, German/English note naming | `controller.conf`; no note history | Missing/ambiguous input reports an error; UI still opens | Command note-on and note-off are consumed; natural and sharp keyboard regions remain distinct; mapped CCs are blocked until pickup crosses the loaded value; exact held-note counts | `midi.rs`, `chord.rs`, `control.rs`, `engine.rs`, `ui.rs`; pickup, pad, naming, held-note, color/layout tests | `shr-daw-playback.png` |
| Controller profiles and non-audible learn | Match MiniLab 3 or learn controls one physical action at a time | CLI/setup plus learned C | Bundled/user controller catalog and `controller.conf` | User catalog and private learned map | Unknown controller becomes unmapped musical input; conflicts are errors | Learn never forwards to a synth; backups precede replacement; update validates size/schema then writes atomically | `controller_profile.rs`, `controller_learn.rs`, `pads.rs`; conflict, matching, backup tests | Physical controller shot |
| Spatial controller navigation | Four menu pages and four items expose every active workflow; encoder moves/selects | K/M/C; 4/5/8-button layouts | `menu.layout`, pad/CC roles, relative encoder direction | Current mapping; screen/context selection is runtime only | Empty actions/pages are hidden and skipped | Panic and exit/back remain reachable in modal contexts; MIDI cannot quit the app | `navigation.rs`, `pads.rs`, `ui.rs`; reachability and 40-column rendering tests | Any 40×20 screen with strip |
| MIDI Ideas | Record free-timed musical MIDI, save with sound identity/values, inspect, reload, and replay | K/M/C; CLI list/inspect/play/delete | State/data paths and active engine | Private directory with `recording.mid`, metadata, preset reference, and synthv1 snapshot | Missing external bank or malformed/oversized owned files fail visibly | Atomic non-replacing directory publish; fsync before publish; safe names; validated events; stop sends All Notes Off; not audio | `recording.rs`, `ui.rs`, `main.rs`; SMF, bounds, path, persistence tests | `shr-daw-playback.png` or Ideas screen |
| FT2 step editing and cell editor | Enter notes/chords, blanks, note-offs, gates, velocity, program, cut/delay/retrigger/tempo | K/M/C plus musical MIDI | Project timing/gate defaults, controller input | Cells inside `.shsong` | Invalid drafts stay uncommitted with an error | Transactional edit/cancel; bounded commands; four lanes per page; no XM import and not an FT2 clone | `sequencer.rs`, `ui.rs`; cell validation, gesture, render tests | `shr-daw-ft2-pattern.png` |
| Play / Rec / Edit / N00B modes | Normal transport, hardware-only quantized capture, step entry, or nearest major/minor note mapping | K/C plus musical MIDI | Current Pattern/page route and scale choice | REC/EDIT cells persist when Project saves; scale setup is runtime | REC refuses Active Instrument and unavailable routes report errors | Source channel/note and repeated-note ownership; command pads consumed; mode/stop/panic cancel exact notes | `scale.rs`, `ui.rs`, `engine.rs`; repeated note, scale, record route tests | Tracker plus mode strip |
| Pattern pages, columns, and external instruments | Each page chooses one destination; four columns choose channel, bank, program; profiles add names | K/M/C and performance MIDI | `external_midi.*`, `.shsong`, device-profile JSON | Routes in Project; user profile overrides under XDG data | Offline names remain saved; ambiguous exact/partial ports are refused, never guessed | Shared target/channel allowed only with identical bank/program; route changes release affected owners | `sequencer.rs`, `device_profile.rs`, `ui.rs`; profile, collision, migration, ambiguity tests | `shr-daw-ft2-pages.png` |
| Sequencer and Arrangement | Schedule all enabled pages, play from cursor/start, mute, stop, and chain Pattern IDs | K/M/C | Pattern tempos, setup messages, send-transport/live-thru options | Pattern map and ordered references in `.shsong` | Available pages continue if another target fails; status names offline targets | Destination/channel/note/lane ownership prevents early note-off; stop/mute cleanup; setup limits; no audio time line | `sequencer.rs`, `ui.rs`; scheduling, shared-note, mute, tempo tests | `shr-daw-ft2-arrangement.png` |
| Project and Pattern storage | Save/load/rename/copy Projects; create/clone/paste/clean Patterns without confusing data and order | K/M/C | XDG data song directory | Versioned, readable `.shsong` files | Unknown/newer versions and malformed shapes are refused and not overwritten | 16 MiB decoded format cap; max patterns/steps/cells; atomic writes; collision refusal; clean deletes only unreferenced Pattern | `sequencer.rs`, `fsutil.rs`, `ui.rs`; round-trip, migration, limit, rename tests | `shr-daw-project-files.png` |
| Drum library and transpose | Filter 72 original seeds, expand to 2/4/8 bars, load/save percussion page, transpose melodic pages | K/M/C | Bundled catalog/files; XDG user drum directory | User `.shdrum` files; bundled data read-only | Malformed entries are skipped; resize refuses to destroy existing melodic data | Four lanes; route unchanged; only user saves deletable; genre labels are creative hints, not authoritative transcriptions | `drum_pattern.rs`, `sequencer.rs`, `ui.rs`; 72-entry, arrange, transpose tests | `shr-daw-drum-patterns.png` |
| WAV loop player and private library | Import mono/stereo WAV, estimate pulse, set BPM interpretation, trim/align, follow FT2 transport | K/M/C | Import inbox, JACK client/outputs; Project loop settings | Copied private WAV plus filename/settings in Project | Corrupt/unsupported WAV errors before JACK; missing JACK or rate mismatch leaves tracker usable | Native speed/pitch only; exact JACK sample-rate match; 6,000,000-frame decode limit; callback has immutable PCM/no locks/I/O; delete only unreferenced regular files | `loop_player.rs`, `ui.rs`; decode, alignment, callback, library safety tests | `shr-daw-ft2-loop.png` |
| Stereo audio recorder | Capture the first configured JACK stereo pair to 24-bit WAV and display rate/size/drop status | K/M/C | `capture.client`, directory, repeated pairs, ring frames | Final `.wav`; owned `.wav.part` while active | Missing JACK/ports/disk space reports error; interrupted recognized files recover on next start | Fixed two-channel SPSC callback; additional configured pairs are not yet selectable; no multitrack or software monitoring; clamped PCM; RIFF 4 GiB cap; `.part` symlinks never followed | `audio_recorder.rs`, `ui.rs`; ring, header, failure, recovery tests | `shr-daw-audio-recorder.png` |
| Owned effects graph and rack | Process the managed source through ordered source inserts, two independent pre/post wet aux sends/returns, then master effects and the final meter | K/M/C | `audio.graph.*`; strict Project effect/routing data | Versioned racks, stable IDs, named parameters, sends, returns, and master order in `.shsong` | Validation/publication failure retains or restores direct playback; a disabled graph accepts validated Project-only edits without processing them | One managed source only; 8 effects/rack, 16 total, 2 reverbs; forced-wet auxes; exactly-once returns; click-conscious bypass/tails; bounded allocation-free callback; active-graph FX changes require stopped transport/recording; direct fallback | `audio_graph*.rs`, `effects/`, `sequencer.rs`, `ui.rs`; schema, DSP, publication, migration, render, and checkpoint tests | FX rack/editor |
| Passive performance meter | Inspect CPU0–CPU3 and the owned graph's final live stereo RMS, short peak markers, independent session `MAX` peaks, and clip state | K/C | Optional CPU temperature path; active graph final meter | Presentation holds only; RESET or any mapped synthv1 Volume-down movement clears both maxima, including before pickup; RESET does not change Project/audio state | Direct/stopped/new sessions clear old maxima and say unavailable instead of changing routes or inventing levels | CPU is whole-core `/proc/stat` load, not process/callback/xrun measurement; audio is post-master owned output only, excluding WAV loops, inputs, hardware, and unrelated clients | `performance_meter.rs`, `audio_graph_runtime.rs`, `ui.rs`; thresholds, independent non-decaying maxima, reset triggers, lifecycle, availability, and 40×20 tests | `shr-daw-performance-meter.png` |
| Local and LAN help | Show compact help at 38 columns; temporarily serve the same content at `/help` | `?`/F1, K/C links; browser optional | Port 80 is fixed; no persistent setting | None | Local help always works if bind/network fails; status explains LAN result | Server exists only while Help is open, has bounded requests/timeouts, and serves one escaped page | `help.rs`, `ui.rs`; HTML, timeout, navigation tests | Help screen |
| Setup, doctor, install, and audio tuning | Install dependencies/files, initialize routes, diagnose ports, optionally reserve a Pi CPU | Shell/CLI, physical choices by user | Repository/install templates and private state | Config/backups; optional tool-owned systemd/boot state | Setup does not start JACK; doctor reports absent services/hardware; uninstall leaves XDG user data | Wizard validates values and backs up; tuner refuses foreign settings and is reversible; system changes require explicit user action | `config.rs`, `main.rs`, scripts, Makefile; parser/unit tests plus ShellCheck/staging checks | Physical connections image |
| Public/private and licence boundary | Package only cleared code/data while retaining private projects, loops, ideas, recordings, and banks | Development/publishing workflow | `.gitignore`, allowlist, installer manifest | `user/` and XDG state/data remain private | Missing optional content is reported, not copied or invented | No tracked `user/`; 21-preset manifest; 424 private presets and uncleared archive excluded; imported loops are private until rights are known | `THIRD_PARTY.md`, `WORKSPACE_HANDOFF.md`, Makefile, tests/checks | README and repository tree |
| Deterministic presentation renderer and visual manual | Exercise real 40×20 drawing with 25 seeded scenarios; generate README visuals and 80 controller-page images | CLI `shr screenshots`; explicit Python renderer for maintainers | None | JSON or regenerated images only when explicitly redirected/run | No JACK/MIDI required | Does not fake audio or hardware results; representative data is labelled presentation data; `--check` detects drift without writing | `ui.rs`, `render-readme-screenshots.py`; screenshot, scenario-count, image-inventory, and geometry tests | Complete menu manual |

Known product boundaries: Debian/Linux is the supported build family; real
audio requires an already running JACK server; actual MIDI routing requires the
named device; loop playback does not time-stretch; external input uses hardware
direct monitoring because software monitoring is absent; one managed software
synth runs at a time; the effects graph is not a multi-source mixer; there is no
native sampler, multitrack audio timeline, or XM compatibility.
